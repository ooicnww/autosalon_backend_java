version: '3.9'

services:
  postgres:
    image: postgres:15
    container_name: storage_db
    environment:
      POSTGRES_DB: storage_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432"